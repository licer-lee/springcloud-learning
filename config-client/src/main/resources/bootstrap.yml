spring: 
  application: 
    # name 必须和git仓库中配置文件名前半部分相同，否则读取不到文件
    name: config-client
  cloud: 
    config: 
#      uri: http://localhost:1201/
#      连接加密的配置中心
      uri: http://localhost:1202/ 
      profile: default
      label: master

server: 
  port: 2001
  